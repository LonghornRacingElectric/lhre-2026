load("@rules_cc//cc:cc_binary.bzl", "cc_binary")

cc_binary(
    name = "sil",
    deps = [
        "@fmilib//:fmilib",
    ],
    srcs = ["main.cpp"],
    copts = ["-std=c++17"],
    linkstatic = True,
    linkopts = select({
        "@platforms//os:windows": ["-lshlwapi"],
        "//conditions:default": [],
    }),
)
