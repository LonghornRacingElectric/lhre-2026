generator client {
  provider = "prisma-client-js"
  output   = "../generated/telemetry-client"
}

datasource db {
  provider = "postgresql"
  url      = env("TELEMETRY_DATABASE_URL")
}

// Telemetry models will be added here after introspection
