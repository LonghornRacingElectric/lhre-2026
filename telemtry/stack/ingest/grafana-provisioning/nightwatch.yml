apiVersion: 1

datasources:
  - name: Telemetry DB (Postgres)
    uid: telemetry
    type: postgres
    access: proxy
    isDefault: false
    url: db:5432
    user: electric
    secureJsonData:
      password: ${POSTGRES_PASSWORD}
    jsonData:
      database: 'telemetry'
      sslmode: disable

