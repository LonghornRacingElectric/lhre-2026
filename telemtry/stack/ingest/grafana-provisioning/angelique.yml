apiVersion: 1

datasources:
  - name: Angelique
    uid: angelique
    type: postgres
    access: proxy
    isDefault: false
    url: db:5432
    jsonData:
      database: 'angelique'
      sslmode: 'disable'
    user: electric
    secureJsonData:
      password: ${POSTGRES_PASSWORD}

