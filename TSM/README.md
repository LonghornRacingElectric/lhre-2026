The Thermal Sensor Module (TSM) measures the ambient temperature near the radiator such that the radiator fan speeds can be properly set and controlled.

TSM Firmware will live in the `TSM/firmware` directory.

This directory can be recursively built by running `bazel build //TSM/firmware/...`.
